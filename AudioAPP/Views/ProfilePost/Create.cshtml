@model AudioAPP.ViewModels.ProfileViewModel

@{
}
<h1>Create</h1>

<div class="container p-3 border">
    <form asp-controller="ProfilePost" asp-action="Create" method="post" enctype="multipart/form-data">
        <div class="mb-3">
            <label asp-for="Title">Tytuł posta</label>
            <input asp-for="Title" />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Description">Treść posta:</label>
            <input asp-for="Description" />
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>
@*        <select class="form-control" asp-for="Priorities"asp-items= "@Html.GetEnumSelectList<Priority>()"></select>*@
        <button type="submit" class="btn btn-primary">Dalej</button>
    </form>
</div>